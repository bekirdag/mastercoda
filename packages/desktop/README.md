# Master Coda Desktop

Initial Electron shell for the Master Coda desktop client.

## Local development

```bash
npm install
npm run dev
```

The renderer lives in `src/renderer` and will later host the UI slices from `react_app`.
